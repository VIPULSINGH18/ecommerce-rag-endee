# Endee Vector Database
ENDEE_BASE_URL=http://localhost:8080/api/v1
ENDEE_AUTH_TOKEN=          # Leave empty if running Endee without auth

# OpenAI (optional – enables natural-language RAG answers in /ask)
OPENAI_API_KEY=sk-...      # Leave empty to use the built-in fallback summariser
